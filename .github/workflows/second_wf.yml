name : second
on : [ push ]
jobs :

  job1 : 
  name : first
  runs-on : ubuntu-latest
  steps : 
  - name : so
  uses : actions/checkout@v2
  run : 
   - name : st
      run : env | sort
  
  job2 : 
  name : second
  runs-on : windows-latest
  steps : sso
  uses : actions/checkout@v2
  name - sst
  run : "Get child"
